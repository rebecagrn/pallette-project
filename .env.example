# Server-side URL for the FastAPI backend (used by Next.js API routes)
PALETTE_API_URL=http://localhost:8000

# Optional shared secret sent as X-Palette-Api-Secret (must match backend/.env)
PALETTE_API_SECRET=
